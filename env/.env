# Non-sensitive GitHub OAuth config (committed).
# Secrets (VITE_GITHUB_CLIENT_ID, VITE_GITHUB_CLIENT_SECRET) live in env/.env.local
# locally and in repo secrets in CI — never commit them.
VITE_GITHUB_SCOPE=read:user repo read:discussion write:discussion actions:read actions:write workflow
